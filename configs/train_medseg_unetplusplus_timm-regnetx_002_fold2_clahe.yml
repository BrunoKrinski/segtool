augmentation:
  augmentation_prob: 0.1
  augmentations:
  - clahe
dataset:
  labels: datasets/medseg/labels.txt
  train: datasets/medseg/train/train_ids2.txt
  valid: datasets/medseg/train/valid_ids2.txt
general:
  dataset: medseg
  experiment: Clahe
  gpu: 0
  mode: train
  num_workers: 4
model:
  batch_size: 8
  decoder: unetplusplus
  encoder: timm-regnetx_002
  height: 256
  learning_rate: 0.0001
  num_epochs: 100
  width: 256
