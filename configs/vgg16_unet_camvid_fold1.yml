general:
  mode: train # train or eval
  num_workers: 8
  experiment: baseline_vgg16unet
  dataset: camvid

model:
  encoder: vgg16
  decoder: unet
  batch_size: 8
  num_epochs: 50
  learning_rate: 0.001
  height: 256
  width: 256

dataset:
  train: datasets/camvid/train/train_ids1.txt
  valid: datasets/camvid/train/valid_ids1.txt
  labels: datasets/camvid/labels.txt
