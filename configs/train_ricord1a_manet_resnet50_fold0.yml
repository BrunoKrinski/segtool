dataset:
  labels: datasets/ricord1a/labels.txt
  train: datasets/ricord1a/train/train_ids0.txt
  valid: datasets/ricord1a/train/valid_ids0.txt
general:
  dataset: ricord1a
  experiment: baseline
  mode: train
  num_workers: 8
model:
  batch_size: 8
  decoder: manet
  encoder: resnet50
  height: 512
  learning_rate: 0.001
  num_epochs: 50
  width: 512
