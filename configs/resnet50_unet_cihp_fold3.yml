general:
  mode: train # train or eval
  num_workers: 8
  experiment: baseline_resnet50unet
  dataset: cihp

model:
  encoder: resnet50
  decoder: unet
  batch_size: 8
  num_epochs: 30
  learning_rate: 0.001
  height: 256
  width: 256

dataset:
  train: datasets/cihp/train/train_ids3.txt
  valid: datasets/cihp/train/valid_ids3.txt
  labels: datasets/cihp/labels.txt
