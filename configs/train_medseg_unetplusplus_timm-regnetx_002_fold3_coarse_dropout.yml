augmentation:
  augmentation_prob: 0.2
  augmentations:
  - coarse_dropout
dataset:
  labels: datasets/medseg/labels.txt
  train: datasets/medseg/train/train_ids3.txt
  valid: datasets/medseg/train/valid_ids3.txt
general:
  dataset: medseg
  experiment: CoarseDropout
  gpu: 0
  mode: train
  num_workers: 4
model:
  batch_size: 8
  decoder: unetplusplus
  encoder: timm-regnetx_002
  height: 256
  learning_rate: 0.001
  num_epochs: 100
  width: 256
