general:
  mode: train # train or eval
  num_workers: 8
  experiment: baseline_resnet50unet
  dataset: gbn_region_patched

model:
  encoder: resnet50
  decoder: unet
  batch_size: 8
  num_epochs: 30
  learning_rate: 0.001
  height: 256
  width: 256

dataset:
  train: datasets/gbn_region_patched/train/train_ids3.txt
  valid: datasets/gbn_region_patched/train/valid_ids3.txt
  labels: datasets/gbn_region_patched/labels.txt
