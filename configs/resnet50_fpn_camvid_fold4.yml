general:
  mode: train # train or eval
  num_workers: 8
  experiment: gaussblur_resnet50fpn
  dataset: camvid

model:
  encoder: resnet50
  decoder: fpn
  batch_size: 8
  num_epochs: 50
  learning_rate: 0.001
  height: 256
  width: 256

dataset:
  train: datasets/camvid/train/train_ids4.txt
  valid: datasets/camvid/train/valid_ids4.txt
  labels: datasets/camvid/labels.txt
