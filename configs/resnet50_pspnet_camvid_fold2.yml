general:
  mode: train # train or eval
  num_workers: 8
  experiment: baseline_resnet50pspnet
  dataset: camvid

model:
  encoder: resnet50
  decoder: pspnet
  batch_size: 8
  num_epochs: 50
  learning_rate: 0.001
  height: 256
  width: 256

dataset:
  train: datasets/camvid/train/train_ids2.txt
  valid: datasets/camvid/train/valid_ids2.txt
  labels: datasets/camvid/labels.txt
