general:
  mode: train # train or eval
  num_workers: 8
  experiment: baseline_resnet50unet
  dataset: gbn_caract_patched

model:
  encoder: resnet50
  decoder: unet
  batch_size: 8
  num_epochs: 100
  learning_rate: 0.0001
  height: 256
  width: 256

dataset:
  train: datasets/gbn_caract_patched/train/train_ids4.txt
  valid: datasets/gbn_caract_patched/train/valid_ids4.txt
  labels: datasets/gbn_caract_patched/labels.txt
