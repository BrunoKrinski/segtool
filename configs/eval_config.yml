general:
  mode: eval # train or eval
  num_workers: 1
  path: RUNS/resnet50_unet_gbnpatched_fold0_2020-12-25_18:59:20.853796

model:
  encoder: resnet50
  batch_size: 8
  height: 256
  width: 256
  type: best # best or last

dataset:
  test: datasets/gbn_patched/valid/test_ids.txt
  labels: datasets/gbn_patched/labels.txt
