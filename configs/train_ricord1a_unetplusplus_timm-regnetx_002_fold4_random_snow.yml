augmentation:
  augmentation_prob: 0.1
  augmentations:
  - random_snow
dataset:
  labels: datasets/ricord1a/labels.txt
  train: datasets/ricord1a/train/train_ids4.txt
  valid: datasets/ricord1a/train/valid_ids4.txt
general:
  dataset: ricord1a
  experiment: RandomSnow
  gpu: 1
  mode: train
  num_workers: 4
model:
  batch_size: 8
  decoder: unetplusplus
  encoder: timm-regnetx_002
  height: 256
  learning_rate: 0.0001
  num_epochs: 100
  width: 256
